'use strict';

//initialize calendar
writeMonth(currentMonth);
setNewDate();
writeEventsOfTheDay(today);
writeDayWeek(today);
// addDotsToCalendar();

//initialize reminders
activateReminders(reminders)
