'use strict';

showCalendar = (month, year) => { }

getDaysInMonth = (month) => { }

getCalendarLimit = (params) => { }

writeMonth = (month) => { }

startDay = () => { }

isLeap = () => { }

setNewDate = () => { }

getNextMonth = (month) => { }

getPrevMonth = (month) => { }