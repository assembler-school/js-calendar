'use strict';

// showEvents = (events) => { }

// showEvent = (event) => { }

// editEvent = (event) => { }

// setEvent = (event) => { }

// getEvent = (event) => { }

// deleteEvent = (event) => { }